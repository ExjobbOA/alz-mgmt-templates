# T10 — K11 Livscykeluppdatering

**Test ID:** T10
**Criterion:** K11 Livscykeluppdatering
**Executed by:** Oskar
**Start date:** 2026-04-21
**End date:** _YYYY-MM-DD_
**Engine tag produced:** `v1.1.1`
**Tenant:** Oskar test tenant (`3aadcd6c-3c4c-49bc-a9d5-57b7fbf31db7`)

---

## Syfte

Visa att en uppdatering av ALZ-biblioteket absorberas av plattformen utan manuella
tillståndsedit. Kapitel 5 i thesisen står för det akademiska resonemanget kring K11; detta
protokoll fångar bara operatörsledet och evidensen.

**Bump:** `2025.09.2` + lokal EncryptTransit-patch → `2026.04.0`
**actionOnUnmanage:** `DetachAll` → `DeleteAll` på governance-stackarna (logging och
networking lämnas på `DetachAll` p.g.a. data-bärande resurser).

---

## Context

Baselinen är ren (11/11 succeeded) men bär en känd avvikelse: `Enforce-EncryptTransit`-
varianterna togs bort lokalt ur `lib/alz/` för att kringgå en case-sensitivity-bugg som
blockerade deploys i `2025.09.2`. Enligt Microsofts PR #308-kommentar var endast den
oversionerade varianten faktiskt trasig; de versionspinnade varianterna fungerade alltid
korrekt eftersom de är pinned till specifika policy definition-versioner. Uppströmsreaktionen
i `2026.04.0` är att ta bort den oversionerade varianten helt (tillsammans med en bredare
deprecation cleanup), vilket automatiskt korrigerar operatörens överreaktion vid
regenereringen.

Denna körning absorberar två uppströmsförändringar i en rörelse:

1. Nya/ändrade definitioner och assignments i `2026.04.0`
2. Borttagning av deprekerade policies (som med `DeleteAll` nu faktiskt raderas, inte
   orphan-detachas)

---

## Phase 0 — Pre-flight

### 0.1 Baseline

- Alla 11 deployment stacks: `succeeded` (verifierat via `Export-ALZStackState.ps1`)
- Engine-tag baseline pekar på: [v1.0.5-baseline-encrypttransit-removed](https://github.com/ExjobbOA/alz-mgmt-templates/releases/tag/v1.0.5-baseline-encrypttransit-removed)
- Lokal patch-commit (EncryptTransit borttagen): `3144344`

### 0.2 Release notes för 2026.04.0

Source: https://github.com/Azure/Azure-Landing-Zones-Library/releases/tag/platform%2Falz%2F2026.04.0

**Nyckelpunkter:**
- PR #308: systematisk borttagning av deprekerade/superseded policies för att undvika
  upprepning av buggar som `Enforce-EncryptTransit` (refererar issues #4128 och #411)
- `Enforce-EncryptTransit` (oversionerad) tas bort helt; versionspinnade varianter
  (`_20240509`, `_20241211`) behålls
- Mellanliggande release `2026.01.3` introducerade nya parameter-defaults för RG location,
  RG names och security contact — kan överlappa med existerande overrides i tenant-repot
- Senaste tillgängliga version vid körning: `2026.04.2` (24 apr) — exkluderad från denna
  körning för att hålla scope fokuserat

![Release notes](tests/screenshots/t10-release-notes.png)

### 0.3 Baseline snapshot

```powershell
.\scripts\Export-ALZStackState.ps1 `
  -OutputFile .\state-snapshots\state-t10-baseline.json `
  -SubscriptionId "6f051987-3995-4c82-abb3-90ba101a0ab4" `
  -TenantIntRootMgId "3aadcd6c-3c4c-49bc-a9d5-57b7fbf31db7"
```

**File:** `state-snapshots/state-t10-baseline.json`
**Timestamp:** `2026-04-24T12:07:01`
**Total managed resources:** 719 fördelat på 11 stackar

**Kända anomalier i baseline (verifieras mot after-state i Phase 3):**
- `Enforce-EncryptTransit_20241211` och `Enforce-EncryptTransit_20240509` ligger som
  `DetachedResources` i governance-int-root-stacken. Bumpen + DeleteAll förväntas radera
  dessa helt.

---

## Phase 1 — Engine repo commits

### 1.1 Metadata bump

`templates/core/governance/tooling/alz_library_metadata.json`:
```diff
- "ref": "2025.09.2"
+ "ref": "2026.04.0"
```

### 1.2 Regenerate library

```powershell
cd templates/core/governance/lib
Remove-Item -Path ".\alz" -Recurse -Force
cd ..\tooling
.\alzlibtool.exe generate architecture "." alz --for-alz-bicep -o "..\lib"
```

### 1.3 Update Bicep references

```powershell
.\Update-AlzLibraryReferences.ps1 -WhatIf   # preview
.\Update-AlzLibraryReferences.ps1            # apply
```

### 1.4 Switch actionOnUnmanage to DeleteAll

`.github/workflows/cd-template.yaml` — governance-stackarna flippas till `DeleteAll`.
Logging och networking behålls på `DetachAll`.

### 1.5 Git diff — klassificering av uppströmsändringar

Rå output: `tests/evidence/t10-git-diff-stat.txt` (48 filer, 27 insertions, 42 deletions)

**20 filer borttagna** — deprekerade/oversionerade policies som 2026.04.0 rensat ut.
Direkt bevis för release-notens claim (PR #308: "removing a number of deprecated and
superseded policies to avoid breaking changes like seen with `Enforce-EncryptTransit`").

| Kategori | Filer |
|---|---|
| Oversionerade policy sets ersatta av versionerade | `Deploy-MDFC-Config`, `Deploy-Sql-Security`, `Enforce-Guardrails-KeyVault`, `Enforce-Guardrails-Network`, `Deploy-ASC-Monitoring-Tp` (set + assignment), `Deploy-Diagnostics-LogAnalytics` (set) |
| Deprekerade policy definitions | `Audit-Kubernetes-ApprovedHostNetworkAndPorts`, `Deny-MachineLearning-PublicNetworkAccess`, `Deny-PublicEndpoint-MariaDB`, `Deny-PublicIP`, `Deny-RDP-From-Internet`, `Deny-Storage-minTLS` |
| MDFC SQL-varianter konsoliderade | `Deploy-MDFC-Arc-SQL-DCR-Association`, `Deploy-MDFC-Arc-Sql-DefenderSQL-DCR`, `Deploy-MDFC-DefenderSQL-AMA`, `Deploy-MDFC-SQL-AMA`, `Deploy-MDFC-SQL-DefenderSQL-DCR`, `Deploy-MDFC-SQL-DefenderSQL` |
| NSG flow log-varianter konsoliderade | `Deploy-Nsg-FlowLogs-to-LA`, `Deploy-Nsg-FlowLogs` |

**6 filer nya:**

| Fil | Typ |
|---|---|
| `Audit-AKS-kubenet` | Ny policy definition |
| `Deploy-ASC-Monitoring` | Ersättning för `Deploy-ASC-Monitoring-Tp` |
| `Deploy-MCSB2-Monitoring` | Ny (Microsoft Cloud Security Benchmark 2) |
| `Enforce-EncryptTransit_20240509` | Återinförd — versionerad variant |
| `Enforce-EncryptTransit_20241211` | Återinförd — versionerad variant |
| `Enforce-Guardrails-KeyVault_20260203` | Versionerad ersättning för oversionerad |

**~15 filer modifierade** — minimala schema-/metadatauppdateringar från 2026.04.0
(±2 rader per fil). Inga innehållsändringar som kräver manuell operatörsgranskning.

### 1.6 Commits

Bump-commit: `dbfe976` — "chore: bump ALZ library 2025.09.2 -> 2026.04.0"
Fix-commit (Update-AlzLibraryReferences på int-root): _paste SHA_
DeleteAll-commit (ingår i `cd-template.yaml`): 12e5e09

Alla commits finns på main och taggats som `v1.1.1`.

---

## Phase 2 — Deploy

### 2.1 Engine repo tag

**Tag:** [`v1.1.1`](https://github.com/ExjobbOA/alz-mgmt-templates/releases/tag/v1.1.1)

### 2.2 Tenant PR (ENGINE_REF bump)

**PR URL:** _paste_
`ENGINE_REF` uppdaterat från `v1.0.5-baseline-encrypttransit-removed` → `v1.1.1`.

### 2.3 What-if output (primärt K11-bevis)

CI:n kör what-if automatiskt vid PR mot tenant-repot. Förväntat output:
- **Creates:** 6 nya filer (Audit-AKS-kubenet, Deploy-ASC-Monitoring, Deploy-MCSB2-Monitoring, 2 EncryptTransit-varianter, Enforce-Guardrails-KeyVault_20260203)
- **Deletes:** 20 borttagna policies — endast policyer/assignments, inga non-policy-resurser (vnet, LAW, RG ska INTE vara i delete-listan)
- **Modifies:** ~15 policies med schemauppdateringar

**What-if URL:** _paste CI run URL_

Hela what-if-outputen sparas som evidens innan cd.

**Observationer från what-if:**
_Fyll i efter körning — särskilt oväntade deletes eller parametervalideringsfel._

### 2.4 CD run (efter merge)

**Run URL:** _paste_
**Result:** _green / red_
**Duration:** _paste_

### 2.5 After-snapshot (sekundär verifikation)

```powershell
.\scripts\Export-ALZStackState.ps1 `
  -OutputFile .\state-snapshots\state-t10-after.json `
  -SubscriptionId "6f051987-3995-4c82-abb3-90ba101a0ab4" `
  -TenantIntRootMgId "3aadcd6c-3c4c-49bc-a9d5-57b7fbf31db7"

.\scripts\Compare-ALZStackState.ps1 `
  -BaselinePath .\state-snapshots\state-t10-baseline.json `
  -CurrentPath .\state-snapshots\state-t10-after.json
```

---

## Phase 3 — Resultat

### 3.1 Förväntat vs observerat

| Förväntat | Observerat | Källa |
|---|---|---|
| What-if förutspådde 6 creates, 20 deletes, ~15 modifies | | CI what-if log |
| CD deployade enligt what-if-prognos, 11/11 succeeded | | CD run log |
| EncryptTransit_20241211 borta från Detached *och* borta ur managed | | after-snapshot |
| Inga manuella tillståndsedit mellan PR merge och CD completion | | CD-loggar |

### 3.2 Observationer

_Fyll i efter körning. Allt som var oväntat — parametervalidation, nya required params,
assignments som failade — noteras här._

### 3.3 Verdict

- [ ] K11 Passed
- [ ] K11 Partially passed
- [ ] K11 Not passed

**En-meningskommentar:** _paste efter körning_

---

## Evidens-artefakter

1. `state-snapshots/state-t10-baseline.json` — före
2. `state-snapshots/state-t10-after.json` — efter
3. `tests/evidence/t10-git-diff-stat.txt` — engine-diff
4. CI what-if URL (primärt K11-bevis)
5. CD run URL (green)
6. 2–3 screenshots:
   - `t10-release-notes.png` — release notes för 2026.04.0
   - `t10-cd-run-green.png` — CD-körningen succeeded
   - `t10-encrypttransit-gone.png` — portalvy som visar att EncryptTransit är borta ur
     stacken (bekräftar DeleteAll-beteendet)

---

## Appendix — Command reference

```powershell
# Baseline
.\scripts\Export-ALZStackState.ps1 `
  -OutputFile .\state-snapshots\state-t10-baseline.json `
  -SubscriptionId "6f051987-3995-4c82-abb3-90ba101a0ab4" `
  -TenantIntRootMgId "3aadcd6c-3c4c-49bc-a9d5-57b7fbf31db7"

# Regenerate lib
cd templates/core/governance/lib
Remove-Item -Path ".\alz" -Recurse -Force
cd ..\tooling
.\alzlibtool.exe generate architecture "." alz --for-alz-bicep -o "..\lib"

# Update refs
.\Update-AlzLibraryReferences.ps1 -WhatIf
.\Update-AlzLibraryReferences.ps1

# After + diff
.\scripts\Export-ALZStackState.ps1 `
  -OutputFile .\state-snapshots\state-t10-after.json `
  -SubscriptionId "6f051987-3995-4c82-abb3-90ba101a0ab4" `
  -TenantIntRootMgId "3aadcd6c-3c4c-49bc-a9d5-57b7fbf31db7"
.\scripts\Compare-ALZStackState.ps1 `
  -BaselinePath .\state-snapshots\state-t10-baseline.json `
  -CurrentPath .\state-snapshots\state-t10-after.json

# Tag
git tag -a v1.1.1 -m "ALZ library 2026.04.0 + DeleteAll + bicep refs fixed"
git push origin v1.1.1
```